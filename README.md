# Zion Updates

Public release and Sparkle appcast host for the private `aidenmi8/zion` source repository.

This repository is intentionally public so the Zion app can fetch signed release metadata and downloads.
